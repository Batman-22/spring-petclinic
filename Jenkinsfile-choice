pipeline {
    agent {label 'JDK8'}
    parameters {
        choice(name: 'Goal', choices: ['package', 'clean package'], description: 'Build with a paramter')
    }
    stages{
        stage('clone'){
            steps{
            git checkout
        }
        }
        stage('build'){
            steps{
            sh "mvn $Goal"
        }
        }
    }
}